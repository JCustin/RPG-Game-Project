class_name global_enums extends Node

enum limb_types {head, leg, arm, tentacle, body}
enum limb_direction {forward_only, rear_only, both}
enum combat_direction {forward, rear}
enum damage_type {piercing, slash, blunt, fire, ice, null_dmg}
enum item_types {potion, armor, weapon}
